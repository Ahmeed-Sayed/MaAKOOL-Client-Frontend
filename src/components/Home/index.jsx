import AppSwiper from "./components/Swiper";

// Get the JWT token from cookies

const HomePage = () => {
  // Example: Logging the JWT token to the console

  return (
    <>
      <AppSwiper />
    </>
  );
};

export default HomePage;
